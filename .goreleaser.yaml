version: 2

project_name: honch

builds:
  - id: honch
    main: ./cmd/honch
    binary: honch
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w

archives:
  - id: binaries
    ids:
      - honch
    formats:
      - binary
    name_template: "{{ .Binary }}-{{ .Os }}-{{ .Arch }}"

checksum:
  disable: true

release:
  mode: keep-existing
  replace_existing_artifacts: true

changelog:
  sort: asc
  abbrev: -1
  format: "{{ .Message }}"
  filters:
    exclude:
      - "^Merge "
      - "^docs:"
      - "^test:"
      - "^chore:"
